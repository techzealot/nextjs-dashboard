import { Metadata } from "next";

const Customers = () => {
  return (
    <div>
      <h1>Customers Page</h1>
    </div>
  );
};

export const metadata: Metadata = {
  title: "Customers",
};
export default Customers;
